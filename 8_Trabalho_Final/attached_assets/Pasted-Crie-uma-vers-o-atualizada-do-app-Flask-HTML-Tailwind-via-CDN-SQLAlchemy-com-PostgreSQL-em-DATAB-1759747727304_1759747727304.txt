Crie uma versão atualizada do app Flask (HTML + Tailwind via CDN, SQLAlchemy com PostgreSQL em DATABASE_URL, Flask-Login) adicionando controle de perfis de usuário: crie a tabela Perfil (id PK, nome único) e semente inicial com exatamente três registros: “Administrativo”, “Parecerista” e “Admin”; no modelo Usuario adicione o campo perfil_id (FK para perfil.id) e a relação perfil; atualize as telas de cadastro/edição de usuário para incluir um select obrigatório “Perfil” (carregado a partir da tabela Perfil) e mostre o perfil do usuário logado no header; mantenha autenticação com Flask-Login e aplique autorização por perfil nas rotas e na interface: apenas Admin pode criar/editar/excluir usuários e ver a lista de usuários; Administrativo pode criar e editar formulários nos estágios Iniciado e Em elaboração, enviar para revisão e ver todos; Parecerista pode atuar nos estágios Em elaboração (nota_técnica) e Em revisão (como revisor), aprovar/solicitar ajustes; Admin pode executar todas as ações, inclusive avançar/voltar estágio e editar qualquer registro; nas páginas, oculte/desabilite botões conforme o perfil e retorne 403 quando um perfil sem permissão acessar uma rota; na página inicial (painel) mantenha as colunas Nº do Processo, Data de Recebimento, Prazo, Parecerista Elaborador, Status, com busca/filtro, e o botão “+ Novo Formulário” visível apenas para Administrativo e Admin; ajuste as validações de transição de estágio para checar permissões por perfil; inclua seeds para criar pelo menos um usuário de cada perfil (com senha_hash via werkzeug.security), bem como seeds de Prazo e Status; atualize routes.py, templates de usuários (login.html, register.html/user_form.html, users_list.html), templates de formulários (ocultando ações por perfil), e o README.md com instruções de como definir DATABASE_URL, rodar python app.py, criar/entrar com os usuários seeded e como alterar perfis; mantenha requirements.txt com Flask, Flask_SQLAlchemy, Flask_Login, psycopg2-binary, python-dotenv; ao iniciar, crie as tabelas (se não existirem) e aplique o seed de Perfil garantindo a existência dos três perfis exatamente com esses nomes.