Atualize o projeto Flask (SQLAlchemy + Flask-Login + Tailwind via CDN) com as seguintes alterações: (1) Renomear a aplicação para “Natjus” (título, header, README). (2) A página inicial deve ser a de login: defina login_manager.login_view = "login" e faça / redirecionar para /login; todo o restante do sistema deve exigir autenticação (@login_required em painéis, formulários, histórico e downloads); visitantes não logados não podem ver nenhuma informação. (3) Remover o campo “status” de dentro do Formulário que não representa etapa; o “status” do sistema passa a ser exclusivamente o campo enumerado de etapas status_formulario ∈ {INICIADO, ELABORACAO, REVISAO, PRONTO_ENVIO}; adapte templates e consultas para usar apenas esse enum; se existir a coluna antiga, ignore/migre para não ser exibida nem editável. (4) Adicionar a coluna “Parecerista Elaborador” na tabela/listagem de pareceres: criar em Formulario os campos id_elaborador (FK usuario.id) e elaborador = relationship("Usuario", foreign_keys=[id_elaborador]); popular id_elaborador automaticamente com current_user.id quando o usuário salvar a etapa Em Elaboração pela primeira vez (não sobrescrever se já existir); exibir essa informação nas tabelas “Painel Geral”, “Painel do Parecerista” e “Histórico de Pareceres” na coluna “Parecerista Elaborador” (mostrar nome do usuário ou “Não informado”). (5) Manter regras de permissão por perfil já definidas e refletir na UI: Admin edita tudo; Administrativo edita somente Iniciado e Pronto para envio; Parecerista edita Elaboração e Revisão; botões “Editar/Avançar/Voltar/Salvar” só aparecem quando current_user.can_edit_stage(f.status_formulario). (6) Upload de PDF na “Nota Técnica” já implementado deve continuar: anexo_nota_tecnica_id (FK documento.id), aceitar apenas PDF até 10 MB, salvar em uploads/ e exibir link “Baixar Nota Técnica (PDF)” quando disponível. (7) Painéis: manter /dashboard (Admin e Administrativo veem TODOS os pareceres) e /meus-pareceres (Parecerista e Admin veem apenas os que podem editar — filtrar por status_formulario in ('ELABORACAO','REVISAO') e, se id_elaborador/id_revisor existir, filtrar por usuário logado); Histórico em /historico (todos autenticados) listando todos os registros com filtros (período, status, Nº do processo, parecerista elaborador). (8) Templates: atualizar cabeçalho para “Natjus”, esconder o sistema inteiro atrás do login, incluir a nova coluna Parecerista Elaborador nas tabelas; onde exibíamos “Status”, usar o enum de etapas; ajustar mensagens e badges. (9) Seeds e migração: garantir perfis “Admin”, “Administrativo”, “Parecerista”; criar ao menos um usuário de cada perfil; adicionar as novas colunas (id_elaborador) e preencher retroativamente como NULL; README deve explicar o novo fluxo (login obrigatório), as rotas (/login, /dashboard, /meus-pareceres, /historico), e como o campo Parecerista Elaborador é definido na etapa Em Elaboração.